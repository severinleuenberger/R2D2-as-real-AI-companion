# ROS2 build artifacts
ros2_ws/build/
ros2_ws/install/
ros2_ws/log/

# Python cache
__pycache__/
*.pyc

# VS Code
.vscode/

# Continue.dev configuration and secrets
.continue/

# Audio/Camera test artifacts
tests/audio/*.wav
# tests/camera/*.jpg  # Allow camera test photos to be committed

# Face Recognition training data (protect locally, don't share on GitHub)
data/face_recognition/*/
!data/face_recognition/models/

# Temporary scratch folder (for session logs, build reports, debugging)
_TEMP/
TEMP_*.md  # Root-level temporary markdown files
*.swp
*.swo
*.bak
*.backup
!vpn_config/*.backup  # Allow vpn_config backups (they're already handled by specific rule)

# Log files
log/**/*.log

# VPN Configuration - SENSITIVE (never commit!)
vpn_config/client_wg0.conf
vpn_config/*.key
vpn_config/keys_backup/
vpn_config/*.backup
*.key
*_private.key
*_public.key

# Python virtual environments
r2d2_speech_env/

# Environment variables and secrets (SECURITY)
.env
*.env
.env.*
.env.local

# SQLite database files (user data, not for sharing)
*.db
*.sqlite
*.sqlite3

# R2D2 config directory (if accidentally created in repo)
.r2d2/
